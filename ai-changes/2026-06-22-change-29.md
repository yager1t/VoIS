# AI Change Log — 2026-06-22-change-29

## Reason

Phase 6 of v0.4 finalizes the streaming ASR milestone: bump the project version
to 0.6.0, refresh all user-facing documentation with the new streaming and
background-final-transcription features, and record the updated verification and
coverage results.

## Files changed

- `pyproject.toml` — bumped `version` from `0.5.0` to `0.6.0`.
- `src/__init__.py` — bumped `__version__` from `0.1.0` to `0.6.0`.
- `README.md` — added v0.4 streaming ASR / model warmup feature bullet,
  usage note for enabling streaming via `.env` or the settings window,
  updated test counts and coverage, and updated the development coverage
  command to include `benchmarks`.
- `docs/architecture.md` — marked all v0.4 phases complete (streaming buffer
  and transcriber, App integration with warmup, background final
  transcription, streaming settings UI, latency benchmark harness, and final
  integration/version bump) and set the next planned milestone to v0.5
  (macOS and Linux support).
- `docs/testing.md` — updated the coverage capture note to v0.4 Phase 6,
  refreshed the coverage table and totals from the latest run, updated the
  example coverage commands to include `benchmarks`, and refreshed test counts.
- `src/asr/base.py` — updated `transcribe_streaming` docstring to reflect that
  streaming is implemented and providers may delegate to `transcribe`.
- `src/asr/whisper_provider.py` — updated `transcribe_streaming` docstring to
  clarify that this provider delegates to full-file transcription per chunk.

## Diff summary

```
pyproject.toml              |  1 +  version bump
src/__init__.py             |  1 +  version bump
README.md                   | 17 +++++++++++++++++++++++++++++++++++++++++ docs
src/asr/base.py             |  3 ++ docstring
src/asr/whisper_provider.py |  3 ++ docstring
docs/architecture.md        | 13 +++++++++++++ docs
docs/testing.md             | 64 +++++++++++++++++++++ docs
```

## Risk

- Very low. No application logic was changed; only version strings, docstrings,
  and user-facing documentation were updated.

## Rollback plan

1. Restore from `backups/2026-06-22/post_change_streaming6.tar.gz`.
2. Or run `git revert <commit-hash>`.

## Verification

- `ruff check src tests benchmarks` — passed
- `mypy src` — passed
- `pytest tests/unit tests/integration -m "not smoke and not slow and not requires_model" --cov=src --cov=benchmarks --timeout=60` — 286 passed, 1 deselected, coverage 93.40% (≥ 80% threshold)

## AI metadata

- AI: yes
- Model: kimi
- Change-ID: 20260622-kimi-748746
