# AI Change Log — 2026-06-22-change-7

## Reason
Phase 1 of the test plan requires setting up test coverage infrastructure and
pytest markers before expanding unit tests. This change adds `pytest-cov`,
configures coverage thresholds, registers markers, creates cross-platform test
runner scripts, and records a baseline coverage report.

## Files changed
- `pyproject.toml` — added `pytest-cov` to dev dependencies, pytest markers,
  and `[tool.coverage.run]` / `[tool.coverage.report]` sections.
- `requirements-dev.txt` — added `pytest-cov>=5.0`.
- `.gitignore` — ignored `.coverage` and `htmlcov/`.
- `scripts/run_tests.sh` — bash test runner (lint, type check, pytest+coverage).
- `scripts/run_tests.bat` — Windows cmd test runner.
- `docs/testing.md` — testing guide and baseline coverage table.

## Diff
```diff
. gitignore            |  5 ++++
 docs/testing.md       | 83 +++++++++++++++++++++++++++++++++++++++++++++++++++
 pyproject.toml        | 27 +++++++++++++++++
 requirements-dev.txt  |  1 +
 scripts/run_tests.bat | 16 ++++++++++
 scripts/run_tests.sh  | 13 ++++++++
 6 files changed, 145 insertions(+)
```

## Risk
- Coverage `fail_under = 80` causes the coverage command to exit non-zero while
  baseline coverage is 70%. This is intentional; Phase 1 will add tests to reach
  the threshold.
- No production code changed, so runtime behavior is unaffected.

## Rollback plan
- Revert commit `419f9df` or restore from `backups/2026-06-22/pre_change_testinfra.tar.gz`.

## Verification
- `ruff check src tests` — passed
- `mypy src` — passed
- `pytest tests/ -q` — 58 passed
- `pytest tests/ --cov=src --cov-report=term-missing` — 58 passed, coverage 70%
  (fails `fail_under=80` as expected)

## AI metadata
- AI: yes
- Model: kimi
- Change-ID: 20260622-kimi-4ebded
- Commit: 443dcc1
