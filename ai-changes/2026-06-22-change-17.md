# AI Change Log — 2026-06-22-change-17

## Reason
Phase 5 of v0.2 finalizes the release: bump the project version, update user
and developer documentation to reflect v0.2 features, and run final
verification (lint, type check, tests with coverage).

## Files changed
- `pyproject.toml` — bumped `version` from `0.3.0` to `0.4.0`.
- `README.md` — rewrote feature list, quick start, CLI options, added LLM setup
  section, updated development commands and test/coverage counts.
- `docs/architecture.md` — marked Phase 5 of v0.2 as completed in roadmap,
  added a dedicated "Qt UI layer and threading model" section.
- `docs/testing.md` — updated coverage table and test count after Phase 5,
  refreshed coverage and integration test commands.

## Diff
```diff
diff --git a/pyproject.toml b/pyproject.toml
index 0b2a3c1..9d8e4f2 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -1,6 +1,6 @@
 [project]
 name = "voice-to-cursor"
-version = "0.3.0"
+version = "0.4.0"
 description = "Local-first desktop app for AI-powered voice dictation at the cursor."
```

## Verification
- `ruff check src tests` — passed.
- `mypy src` — passed.
- `pytest tests/unit tests/integration -m "not smoke and not slow and not requires_model" --cov=src --cov-fail-under=80 --timeout=60` — 163 passed, 93.64% coverage.

## Metadata
- AI: yes
- Model: kimi
- Change-ID: 20260622-kimi-c7b86e
