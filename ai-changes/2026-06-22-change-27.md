# AI Change Log — 2026-06-22-change-27

## Reason

Phase 4 of v0.4 exposes the streaming ASR and final-transcription settings in the
existing PyQt6 settings window so users can enable, disable, and tune the new
pipeline behavior without editing `.env` manually.

## Files changed

- `src/ui/settings_window.py` — added a "Streaming ASR" group box with controls for
  `streaming_enabled`, `streaming_chunk_seconds`, `asr_streaming_beam_size`,
  `asr_warmup_at_start`, `final_transcription_enabled`, and
  `replace_streaming_with_final`; loaded and saved the corresponding settings.
- `tests/unit/test_settings_window.py` — added fakes for `QGroupBox` and `QSpinBox`,
  extended load/save assertions to cover the new streaming fields.
- `requirements-dev.txt` — added `PyQt6-stubs` so `mypy src` passes in a clean dev
  environment.
- `pyproject.toml` — added `PyQt6-stubs` to the `dev` optional dependencies.

## Diff summary

```
src/ui/settings_window.py         | +39  streaming ASR group box, load/save wiring
tests/unit/test_settings_window.py | +40  fakes and assertions for streaming settings
requirements-dev.txt              |  +1  PyQt6-stubs
pyproject.toml                    |  +1  PyQt6-stubs
```

## Risk

- Low. UI-only change; no runtime pipeline behavior was modified.
- `replace_streaming_with_final` is kept disabled in the UI and marked as reserved.

## Rollback plan

1. Restore from `backups/2026-06-22/post_change_streaming4.tar.gz`.
2. Or run `git revert <commit-hash>`.

## Verification

- `ruff check src tests` — passed
- `mypy src` — passed
- `pytest tests/unit tests/integration -m "not smoke and not slow and not requires_model" --cov=src --timeout=60` — 267 passed, coverage 93.29% (≥ 80% threshold)

## AI metadata

- AI: yes
- Model: kimi
- Change-ID: 20260622-kimi-9acdd0
- Commit: 4855162
