# AI Change Log — 2026-06-23-change-1

## Reason

Record the checklist verification follow-up: capture the discovered issues,
fix the blocking checklist failures, align CI with the checklist, and refresh
the project description and verification logs.

## Files changed

- `docs/checklist_fix_plan.md` — added a tracker for checklist verification
  issues and marked resolved items after fixes.
- `.github/workflows/ci.yml` — updated CI linting to include `benchmarks` and
  coverage to include both `src` and `benchmarks`.
- `docs/checklist.md` — corrected the safe test command and the streaming test
  path.
- `README.md` — refreshed the project description, streaming ASR settings, and
  current verification summary.
- `docs/architecture.md` — documented streaming beam-size handling and lazy UI
  package exports.
- `docs/testing.md` — refreshed the latest coverage result and clarified CI
  coverage scope.
- `src/ui/__init__.py` — changed UI package exports to lazy loading so settings
  window tests can mock Qt modules without importing tray dependencies.
- `src/asr/base.py`, `src/asr/streaming.py`, `src/asr/whisper_provider.py`,
  `src/benchmarks/latency.py` — routed streaming transcription through
  `transcribe_streaming(..., beam_size=...)` and applied
  `asr_streaming_beam_size`.
- `tests/unit/test_streaming_transcriber.py` and
  `tests/unit/test_asr_provider.py` — added coverage for streaming beam-size
  behavior.
- Multiple files under `src`, `tests`, and `benchmarks` — reformatted with
  `ruff format`.

## Verification

- `ruff check src tests benchmarks` — passed
- `ruff format --check src tests benchmarks` — passed
- `mypy src` — passed
- `pytest tests/unit/test_settings_window.py -v --timeout=60` — 9 passed
- `pytest tests/unit/test_streaming_transcriber.py tests/unit/test_asr_provider.py tests/unit/benchmarks -q --timeout=60` — 43 passed
- `pytest tests/unit tests/integration -m "not smoke and not slow and not requires_model" --cov=src --cov=benchmarks --cov-report=term-missing --timeout=60` — 290 passed, 1 deselected, coverage 92.64%
- `pytest tests/smoke -v --run-smoke --timeout=30` — 1 passed
- `python benchmarks/run_latency.py --mode both` — passed

## Open follow-ups

- Commit: `aef4d72` — `[fix] apply checklist verification fixes and refresh docs/CI`
- Pushed to `origin/main`.
- Recent historical commits vary between `Change-Id:` and `Change-ID:`.
- New backups should use the current date folder if further risky edits require
  another backup.

## AI metadata

- AI: yes
- Model: Codex
- Change-Id: 20260623-codex-checklist-fixes
