# AI Change Log — 2026-06-23-change-2

## Reason

Prepare and publish the v0.6.0 release after the streaming ASR milestone was completed and checklist verification fixes were merged.

## Actions performed

- Verified repository state: clean working tree, version `0.6.0` in `pyproject.toml` and `src/__init__.py`.
- Created annotated Git tag `v0.6.0`.
- Pushed tag `v0.6.0` to `origin`.
- Created GitHub release `Voice-to-Cursor v0.6.0` via `gh release create` with release notes summarizing streaming ASR, benchmarks, and quality improvements.

## Verification

- `git status` — clean working tree, `main` synchronized with `origin/main`.
- `git tag -l` — `v0.6.0` present locally and on origin.
- Release URL: https://github.com/yager1t/VoIS/releases/tag/v0.6.0

## AI metadata

- AI: yes
- Change-Id: 20260623-kimi-release-v060
