# AI Change Log — 2026-06-23-change-5

## Reason

Create the v0.6.1 patch release for Voice-to-Cursor. This release bundles the
windowed-portable crash fix and CUDA fallback fix from `fc261b2` under a new
versioned installer and portable archive.

## Files changed

- `pyproject.toml` — bumped `version` from `0.6.0` to `0.6.1`.
- `src/__init__.py` — bumped `__version__` from `0.6.0` to `0.6.1`.
- `installer/voice-to-cursor.iss` — bumped `MyAppVersion` default from `0.6.0` to `0.6.1`.
- `scripts/build_installer.py` — now derives `VERSION` from `src.__version__` and
  builds `ZIP_NAME` dynamically; passes `/DMyAppVersion={VERSION}` to `iscc.exe`
  so the Inno Setup output filename matches the package version automatically.

## Artifacts

- `dist/installer/Voice-to-Cursor-0.6.1-portable.zip`
- `dist/installer/Voice-to-Cursor-0.6.1-Setup.exe`

## Verification

- Rebuilt installer with `.venv\Scripts\python.exe scripts\build_installer.py`.
- Dry-run launch checks (each terminated after ~10 s):
  - `dist/voice-to-cursor/voice-to-cursor-console.exe --dry-run` — started without crashing.
  - `dist/voice-to-cursor/voice-to-cursor.exe --dry-run` — started without crashing.
- No microphone capture or text injection was performed.
- `dist/` and `build/` remain gitignored and were not committed.

## Backups

- Pre-change: `backups/2026-06-23/pre_change_v061.tar.gz`
- Post-change: `backups/2026-06-23/post_change_v061.tar.gz`

## Commit

- `f5e4197` — `[build] bump version to 0.6.1 and rebuild Windows installer`

## AI metadata

- AI: yes
- Change-Id: 20260623-kimi-v061-release
